package com.vian.vianlauncher

object SidebarManager {
    // PHASE 3 IMPORT — merge FloatBarService/SidebarView from Viansidebhro when ready
}

object NetSpeedManager {
    // PHASE 4 IMPORT — merge NetSpeedManager from Viansidebhro when ready
}

object CallRecorderManager {
    // PHASE 5 IMPORT — merge CallRecorderManager from Viansidebhro when ready
}

object EpubReaderManager {
    // PHASE 6 IMPORT — floating EPUB reader overlay
}

object AppTrackerManager {
    // PHASE 7 IMPORT — app tracker tabs
}

object PwaLoaderManager {
    // PHASE 8 IMPORT — NanoHTTPD PWA loader
}

object LlmServiceManager {
    // PHASE 9 IMPORT — on-device LLM service
}

object SchedulerManager {
    // PHASE 14 IMPORT — AlarmManager scheduler
}
